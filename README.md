It is a repo of my .files
For usage:

git clone https://github.com/kensi-rus/dotfiles ~/.aux
Then go to ~/.aux/scripts and exec ./setup_symlinks.sh
It will replace your current zsh, tmux and nano configs with symlinks to a relevant .aux files.

It has embedded functions conf2git and git2conf for push and pull configs.
